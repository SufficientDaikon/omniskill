name: using-git-worktrees
description: "Git worktree isolation for feature development. Use when starting feature work, experiments, or any multi-file changes that need workspace isolation."
version: "2.0.0"
author: tahaa
license: MIT
category: development-workflow
platforms: [claude-code, copilot-cli, cursor, windsurf, antigravity]
tags: [git, worktree, isolation, branching, feature-development]
triggers:
  keywords: [worktree, git worktree, workspace isolation, feature branch, isolate, isolated workspace]
  patterns: []
priority: P2
