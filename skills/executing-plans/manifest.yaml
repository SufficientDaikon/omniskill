name: executing-plans
description: "Single-session plan execution fallback when subagents unavailable. Use when executing implementation plans without subagent support."
version: "2.0.0"
author: tahaa
license: MIT
category: development-workflow
tags: [execution, fallback, implementation, sequential, single-session]
platforms: [claude-code, copilot-cli, cursor, windsurf, antigravity]
triggers:
  keywords: [execute-plan, run-plan, follow-plan, implement-plan, sequential-execution, single-session]
  patterns: []
priority: P2
